var sitemaps = require('./index');
let i = 0

sitemaps.parseSitemaps('https://www.walmart.com/sitemap_ip.xml', () => {i++}, (err, sitemaps) => {
	if(err) {
		console.log(`err: ${err}`)
	}
    console.log(`All ${i} done`)
});